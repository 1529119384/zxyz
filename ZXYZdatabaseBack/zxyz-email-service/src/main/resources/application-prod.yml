# Redis 配置已由 application-common.yml 统一管理
email:
  host: ${EMAIL_HOST}
  port: ${EMAIL_PORT:587}
  username: ${EMAIL_USERNAME}
  password: ${EMAIL_PASSWORD}
  config-secret: ${EMAIL_CONFIG_SECRET}

app:
  team-service:
    base-url: ${services.team-service.base-url}
