<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.14</version>
        <relativePath/>
    </parent>

    <groupId>uno.acloud</groupId>
    <artifactId>zxyz-backend</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>zxyz-backend</name>
    <description>ZXYZ backend multi-module parent</description>

    <modules>
        <module>zxyz-common</module>
        <module>zxyz-starter</module>
        <module>zxyz-gateway</module>
        <module>zxyz-project-service</module>
        <module>zxyz-im-service</module>
        <module>zxyz-email-service</module>
        <module>zxyz-share-service</module>
        <module>zxyz-file-service</module>
        <module>zxyz-team-service</module>
        <module>zxyz-user-service</module>
        <module>zxyz-audit-service</module>
        <module>zxyz-admin-service</module>
    </modules>

    <properties>
        <java.version>17</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <sa-token.version>1.45.0</sa-token.version>
        <redisson.version>3.35.0</redisson.version>
        <mybatis-plus.version>3.5.9</mybatis-plus.version>
        <springdoc.version>2.8.9</springdoc.version>
        <knife4j.version>4.5.0</knife4j.version>
        <aliyun-oss-v2.version>0.4.1</aliyun-oss-v2.version>
        <commons-lang3.version>3.18.0</commons-lang3.version>
        <lombok.version>1.18.42</lombok.version>
        <!-- 4.0.5 与 Jakarta EE 10（Spring Boot 3.x）兼容；2.3.x 使用 javax 命名空间，不可用于 Spring Boot 3 -->
        <jaxb-runtime.version>4.0.5</jaxb-runtime.version>
        <simple-java-mail.version>8.12.6</simple-java-mail.version>
<!-- m54: MapStruct — 对象映射框架，逐步替代手动字段拷贝 -->
        <mapstruct.version>1.6.3</mapstruct.version>
        <flyway.version>10.22.0</flyway.version>
        <testcontainers.version>1.20.4</testcontainers.version>
        <!-- 2025.0.0 -> 2025.0.3：gateway-server(-webflux) 4.3.0 -> 4.3.5，修复 CVE-2025-41253（HIGH，需 ≥4.3.2）与 CVE-2025-41243（CRITICAL，需 ≥4.3.1）；同 train patch 升级，SCA 2025.0.0.0 兼容 -->
        <spring-cloud.version>2025.0.3</spring-cloud.version>
        <spring-cloud-alibaba.version>2025.0.0.0</spring-cloud-alibaba.version>
        <resilience4j.version>2.2.0</resilience4j.version>
        <jasypt.version>3.0.5</jasypt.version>

        <!-- ===== 安全漏洞修复覆盖：以下属性覆盖 Spring Boot 3.5.14 自带
             dependency management 中仍低于修复版的依赖（Trivy HIGH/CRITICAL）。
             全部为 Spring Boot dependencies 已有的同名属性，仅调高版本号，
             不改 pom 结构。版本均经 Maven Central 200 校验。 ===== -->
        <!-- netty 4.1.132 → 4.1.137.Final：修复大量 CVE（HIGH） -->
        <netty.version>4.1.137.Final</netty.version>
        <!-- tomcat 10.1.54 → 10.1.59：修复 CVE-2026-41293 等（CRITICAL） -->
        <tomcat.version>10.1.59</tomcat.version>
        <!-- jackson 2.21.2 → 2.21.4：GHSA-r7wm-3cxj-wff9 / CVE-2026-54512（2.19 线无修复，升到 2.21.4 修复版；Jackson 2.x 二进制兼容） -->
        <jackson-bom.version>2.21.4</jackson-bom.version>
        <!-- micrometer 1.15.11 → 1.15.12 -->
        <micrometer.version>1.15.12</micrometer.version>
        <!-- httpcore5 5.3.6 → 5.4.3 -->
        <httpcore5.version>5.4.3</httpcore5.version>
        <!-- rabbit amqp-client 5.25.0 → 5.35.0：CVE-2026-63337/69219 -->
        <rabbit-amqp-client.version>5.35.0</rabbit-amqp-client.version>
        <!-- spring-framework 6.2.18 → 6.2.19：spring-expression CVE（≥6.2.19） -->
        <spring-framework.version>6.2.19</spring-framework.version>
        <!-- spring-data-bom 2025.0.11 → 2025.0.13：spring-data-commons 3.5.11 → 3.5.13（≥3.5.12） -->
        <spring-data-bom.version>2025.0.13</spring-data-bom.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-alibaba-dependencies</artifactId>
                <version>${spring-cloud-alibaba.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>uno.acloud</groupId>
                <artifactId>zxyz-common</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>uno.acloud</groupId>
                <artifactId>zxyz-common</artifactId>
                <version>${project.version}</version>
                <type>test-jar</type>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>uno.acloud</groupId>
                <artifactId>zxyz-starter</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-extension</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-jsqlparser</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
            <dependency>
                <groupId>com.github.xiaoymin</groupId>
                <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
                <version>${knife4j.version}</version>
            </dependency>
            <dependency>
                <groupId>com.aliyun</groupId>
                <artifactId>alibabacloud-oss-v2</artifactId>
                <version>${aliyun-oss-v2.version}</version>
            </dependency>
            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-core</artifactId>
                <version>${sa-token.version}</version>
            </dependency>
            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-spring-boot3-starter</artifactId>
                <version>${sa-token.version}</version>
            </dependency>
            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-jwt</artifactId>
                <version>${sa-token.version}</version>
            </dependency>
            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-redis-template</artifactId>
                <version>${sa-token.version}</version>
            </dependency>
            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-reactor-spring-boot3-starter</artifactId>
                <version>${sa-token.version}</version>
            </dependency>
            <dependency>
                <groupId>org.redisson</groupId>
                <artifactId>redisson</artifactId>
                <version>${redisson.version}</version>
            </dependency>
            <dependency>
                <groupId>io.github.resilience4j</groupId>
                <artifactId>resilience4j-spring-boot3</artifactId>
                <version>${resilience4j.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>${commons-lang3.version}</version>
            </dependency>
            <dependency>
                <groupId>org.glassfish.jaxb</groupId>
                <artifactId>jaxb-runtime</artifactId>
                <version>${jaxb-runtime.version}</version>
            </dependency>
            <dependency>
                <groupId>org.simplejavamail</groupId>
                <artifactId>simple-java-mail</artifactId>
                <version>${simple-java-mail.version}</version>
            </dependency>
            <dependency>
                <groupId>org.flywaydb</groupId>
                <artifactId>flyway-core</artifactId>
                <version>${flyway.version}</version>
            </dependency>
            <dependency>
                <groupId>org.flywaydb</groupId>
                <artifactId>flyway-mysql</artifactId>
                <version>${flyway.version}</version>
            </dependency>
            <!-- m54: MapStruct 对象映射框架，逐步替代手动字段拷贝（如 toTeamVO、toMemberVO 等）。
                 各服务模块需同时添加 mapstruct（编译期）和 mapstruct-processor（注解处理器）依赖。 -->
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers</artifactId>
                <version>${testcontainers.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>mysql</artifactId>
                <version>${testcontainers.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>${testcontainers.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>com.github.ulisesbocchio</groupId>
                <artifactId>jasypt-spring-boot-starter</artifactId>
                <version>${jasypt.version}</version>
            </dependency>
            <!-- bouncycastle bcprov-jdk18on 为传递依赖（如经 sa-token 等引入），
                 未在任意 pom 显式声明；此处显式定版以修复 CVE-2025-14813（CRITICAL），
                 覆盖到 ≥1.84（取 1.85，jdk18on 线）。 -->
            <dependency>
                <groupId>org.bouncycastle</groupId>
                <artifactId>bcprov-jdk18on</artifactId>
                <version>1.85</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- 共享依赖：所有模块（含 10 个 app 模块）继承。
         spring-boot-devtools：本地开发热部署（Debug 模式生效），
         optional 不传递、runtime 不参与编译，repackage 自动排除，不影响生产镜像。 -->
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.12</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals><goal>prepare-agent</goal></goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals><goal>report</goal></goals>
                    </execution>
                </executions>
                <configuration>
                    <excludes>
                        <exclude>uno/acloud/*/*Application.class</exclude>
                        <exclude>uno/acloud/*/config/*AutoConfiguration*</exclude>
                        <exclude>uno/acloud/*/config/RestClientConfig.class</exclude>
                        <exclude>uno/acloud/starter/*AutoConfiguration*</exclude>
                        <exclude>uno/acloud/*/config/*Properties.class</exclude>
                        <exclude>uno/acloud/*/config/*DataSourceConfig.class</exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <encoding>${project.build.sourceEncoding}</encoding>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                            <!-- m54: MapStruct 注解处理器，与 Lombok 配合使用 -->
                            <path>
                                <groupId>org.mapstruct</groupId>
                                <artifactId>mapstruct-processor</artifactId>
                                <version>${mapstruct.version}</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <configuration>
                        <excludes>
                            <exclude>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                            </exclude>
                        </excludes>
                        <!-- 禁用 Nacos 客户端内置 logback，避免 Windows 上日志轮转 rename 失败 -->
                        <jvmArguments>-Dnacos.logging.default.config.enabled=false</jvmArguments>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
