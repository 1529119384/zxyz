运行后端测试：`mvn clean test`（JaCoCo 报告位于 `target/site/jacoco/index.html`）；运行前端测试：`npm run test`；Docker 启动顺序：MySQL → Redis → RabbitMQ → Nacos → 业务服务 → Gateway → frontend-nginx；Jasypt 密钥生成：`openssl rand -base64 32` 并通过 `jasypt-spring-boot-starter` 在 `application-common.yml` 中配置。