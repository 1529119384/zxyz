一键部署：`cp .env.example .env` 编辑密钥后执行 `docker compose up -d --build`；快速回滚：修改 `.env` 中 `APP_IMAGE_TAG` 指向旧版本再 `docker compose up -d`；环境校验：`./scripts/validate-env.sh .env` 检查 `CHANGE_ME_*` 占位符；自托管 Runner 安装见 DEPLOYMENT.md 第 3.1 节。