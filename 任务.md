# 待办

### 意见箱

### 文件删除有点问题 具体实现

复制一份空文件夹 然后将原来的文件夹删除到回收站 复制出来的文件夹就会删除失败

### 文件分享未登录

### 所有 MySQL 服务以 `root` 连接

**文件**: `docker-compose.yml:124-126`

**问题**: 每个服务使用 MySQL root 密码。被入侵的服务拥有所有数据库的完全管理员权限。

**修复**: 创建每个服务专用的 MySQL 用户，仅授予最小的 `SELECT/INSERT/UPDATE/DELETE` 权限。

> ✅ **已核实** -- docker-compose.yml 中所有服务确实使用 root 密码连接。

### 文件预览onlyoffice

> 在线预览您的文本、视频、音频、pdf、office、3d 文件.
>
> 支持预览纯文本、Markdown、Java、Python、PHP、Go、JavaScript 等文件.
>
> 支持在线预览 mp4（H.264）、m3u8、flv 编码格式的视频，还提供调用本地播放器进行解码，如 PotPlayer、IINA、VLC 等.
>
> 支持在线音频播放，如 mp3、wav、ogg、flac 等. 并提供播放列表方便切换.
>
> 支持 Word、Excel、PowerPoint、PDF 在线预览.
>
> 支持 dae, fbx, gltf, glb, obj, ply, stl 格式文件在线预览.

### 无关页面重定向

### 文件上传和删除的时候 没有及时刷新大小 

> ✅ **已修复（2026-09-13）** —— 两个**互相独立**的根因，缺一不可：
>
> **1. 后端：用量缓存「声明了但从未接线」**
> - `zxyz-file-service` 的 `StorageCacheService`（key `file:storage:`，TTL 30s）里 `invalidateAllStorageCaches()` **没有任何生产调用点**（只有 `StorageCacheServiceTest` 调用过）⇒ 上传/彻底删除/复制/移动后缓存永不失效。
> - `zxyz-project-service` 的 `StorageQuotaCacheService` 由 MQ 事件 `file.resource.changed` 驱动（队列 `zxyz.project.file-events`），**这条链路本来是通的**；但**上传与复制路径此前从不发布任何事件**。
> - 已接线：`FileLifecycleService`（逻辑删除/彻底删除/恢复）、`FileOperationHelper`（移动/重命名）、`FileCopyService`（复制）、`FileUploadPersistenceManager`（上传落库后发 `CREATED` 事件）。失效统一包在 `TransactionUtils.runAfterCommit(...)`：**提交后**才失效（避免「读旧值又写回缓存」），且该工具**吞异常** ⇒ Redis/MQ 抖动不会把一次成功的上传/删除变成 500（缓存 30s 自然过期兜底）。
> - 复制路径的失效刻意放在 `finally`：批次是独立事务，中途某批失败时**前面批次已经提交**，只在成功路径失效会让这些已落库批次永远停在旧值。
>
> **2. 前端：keep-alive 导致返回首页不重新挂载**
> - `IndexPage` 在 `src/views/layout/index.vue` 的 `keepAliveNames` 里 ⇒ 从回收站/分享管理页返回首页时组件**不重新挂载**，`onMounted` 与空间 `watch` 都不触发 ⇒ 顶部容量条停在旧值。
> - 已在 `src/views/index/index.vue` 加 `onActivated` 补拉用量（用标志位跳过首次，避免与 `onMounted` 重复请求）；移动/复制成功的 `onSuccess` 回调也补拉一次（复制会增加用量，移动不会，但共用一个回调）。
>
> **口径澄清（不是缺陷，未改）**：`SUM(file_size)` 的 SQL 是 `deleted IN (0,1) AND file_type = 1`，即**回收站条目仍占空间**，只有「彻底删除」（`deleted=2`）才释放。所以「删除到回收站后容量不变」是**正确**行为。
>
> **验证**：后端 `mvn -B test -pl zxyz-file-service -am` → **168 tests / 0 failures**，jacoco 门禁通过（新增 `FileUploadPersistenceManagerTest` 4 例，另在生命周期与复制测试各加回归）；前端四道门禁全绿（**991 tests / 覆盖率 91.33% / eslint 0 error / 类型范围棘轮通过**）。
